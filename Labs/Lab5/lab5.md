npx -y @modelcontextprotocol/server-postgres postgresql://postgres:password@localhost:5432/ecommerce_db


{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://postgres:password@localhost:5432/ecommerce_db"
      ]
    }
  }
}

Find out when we do add_to_cart, which API we hit? and which database table is involved?
Prompt